Technical requirements:

HTML
Have at least 3 pages, keep the grid system consistent as much as possible
Use at least 10 different HTML tags
Use HTML tables
Implement at least two uses for forms
Dropped Down Menu
Use web fonts
Use different types of content in the form of text, images, videos, and GIFs
Use regex validation

CSS
Inline, internal, and external styling
Use five different CSS selectors
Don’t use too many fonts
Use colors that complement each other
Use Flexbox (Optional)
Use SASS/SCSS (Optional)
Use animations (Optional)

JavaScript
External scripts
Use variables, if statements, loops, at least one form of collections, functions/callbacks, and events.
Use AJAX (Optional)
Use JSON or XML (Optional)
Use JQuery (Optional)


index.html
This is the meat of the website. The layout is contained here.

home.html
This is the landing page which instructs the user to make use of the dropdown menu

about.html
This page cycles through an array of pre-written facts about me upon the click of a button. It utilizes forms to add additional items to the array that can also be cycled through.

contact.html
This page contains a form that utilizes regex to validate input.
